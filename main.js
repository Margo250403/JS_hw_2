var names = prompt("Enter names:");

var arr = names.split(', ');

for (var arr_name of arr)
{
     console.log("<li>"+ arr_name + "</li>"); // Alex, Vasa, Pitya, Ann, Oleg
}



//  var names = prompt("Enter names:");
//  var arr = names.split(",");
//  for (var index of names)
//  {
//     console.log( + index + 1 + " " + arr_names);
//  }
